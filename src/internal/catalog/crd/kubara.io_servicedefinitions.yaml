---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: servicedefinitions.kubara.io
spec:
  group: kubara.io
  names:
    kind: ServiceDefinition
    listKind: ServiceDefinitionList
    plural: servicedefinitions
    shortNames:
    - sd
    singular: servicedefinition
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceDefinition describes a catalog service entry.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceSpec contains the desired behavior and schema of a
              service.
            properties:
              chartPath:
                description: ChartPath points to the immutable Helm chart path under
                  platform-components/helm/.
                minLength: 1
                type: string
              clusterTypes:
                description: ClusterTypes limits the service to specific cluster types
                  as catalog metadata.
                items:
                  type: string
                type: array
              configSchema:
                description: ConfigSchema describes config values using OpenAPI v3
                  schema props.
                x-kubernetes-preserve-unknown-fields: true
              status:
                description: Status defines the default status for the service and
                  may be overridden per cluster.
                enum:
                - enabled
                - disabled
                type: string
            required:
            - chartPath
            - status
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
