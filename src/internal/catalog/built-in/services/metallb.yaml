---
apiVersion: kubara.io/v1alpha1
kind: ServiceDefinition
metadata:
  name: metallb
  annotations:
    kubara.io/category: networking
spec:
  chartPath: metallb
  status: disabled
  clusterTypes:
    - hub
    - spoke
  configSchema:
    type: object
    properties:
      publicLoadBalancerCIDR:
        type: string
      privateLoadBalancerCIDR:
        type: string
# ToDo: Just boilerplate yet, got to validate and think about it later.
